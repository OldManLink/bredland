name: Tests

on:
  pull_request:
  push:

jobs:
  shell-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run tests
        run: ./tests/run-all.sh
